# Netlify configuration for React Native Web SPA
[build]
  publish = "dist"
  command = "npx expo export -p web && node scripts/fix-web-fonts.js"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200










