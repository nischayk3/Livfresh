/**
 * Base file for TypeScript module resolution.
 * Metro bundler resolves to payment_helper.native.ts or payment_helper.web.ts at runtime.
 */
export const openRazorpay = async (_options: any): Promise<any> => { };
